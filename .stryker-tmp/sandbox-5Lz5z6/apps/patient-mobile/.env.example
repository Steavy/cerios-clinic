# Local development environment (LAN profile)
# Replace LAN_IP with your machine's LAN IP address (run ipconfig to find it)
API_URL=http://LAN_IP:3001/api
KEYCLOAK_URL=http://LAN_IP:8180
KEYCLOAK_REALM=clinic
KEYCLOAK_CLIENT_ID=patient-mobile-client
